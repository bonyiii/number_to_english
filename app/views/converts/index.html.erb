<h1>Listing converts</h1>

<table>
  <tr>
  	<th><%= t ('activercord.attributes.convert.arabic') %></th>
  	<th><%= t ('word') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @converts.each do |convert| %>
  <tr>
  	<td><%= convert.arabic %></td>
  	<td><%= convert.word %></td>
    <td><%= link_to 'Show', convert %></td>
    <td><%= link_to 'Edit', edit_convert_path(convert) %></td>
    <td><%= link_to 'Destroy', convert, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Convert', new_convert_path %>
